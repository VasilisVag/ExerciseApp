<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-feature android:name="android.hardware.camera" android:required="true" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Boilerplate"
        tools:targetApi="31">

        <activity android:name="com.example.ExerciseApp.LoginActivity" />
        <activity android:name="com.example.ExerciseApp.RegisterActivity" />
        <activity android:name="com.example.ExerciseApp.ProfileActivity" />
        <activity android:name="com.example.ExerciseApp.WorkoutActivity" />
        <activity android:name="com.example.ExerciseApp.SettingsActivity" />
        <activity android:name="com.example.ExerciseApp.ScannerActivity" />

        <activity android:name="com.example.ExerciseApp.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>